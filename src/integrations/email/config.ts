/**
 * Centralized "From" address for all transactional Resend sends.
 * When the Resend domain is verified, change this single line.
 */
export const EMAIL_FROM = "JMS Web Studio <onboarding@resend.dev>";
