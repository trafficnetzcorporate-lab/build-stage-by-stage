// Mortgage logo placeholders. Real PNGs replace these once uploaded.
// Logo files live at src/assets/{company}-mortgage-logo.png — currently 1x1 transparent shims.
import * as React from "react";
export const ACRISURE_LOGO_PLACEHOLDER = true;
export const GUILD_LOGO_PLACEHOLDER = true;
// no runtime export
void React;
